# Frontend DOM Traversal Visualizer

React + TypeScript + Vite UI untuk aplikasi traversal DOM.

## Development

```bash
npm install
npm run dev
```

Vite berjalan di `http://localhost:5173` dan proxy `/api` diarahkan ke backend `http://localhost:5080`.

## Build

```bash
npm run build
```
