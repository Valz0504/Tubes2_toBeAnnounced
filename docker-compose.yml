services:
  backend:
    build: 
      context: ./backend
    ports:
      - "5080:8080" 
    restart: always

  frontend:
    build: 
      context: ./frontend
    ports:
      - "5173:80" 
    depends_on:
      - backend
    restart: always