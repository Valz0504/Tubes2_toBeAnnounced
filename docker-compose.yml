services:
  backend:
    build: 
      context: ./backend
    ports:
      - "5080:8080" 
    restart: unless-stopped

  frontend:
    build: 
      context: ./frontend
    ports:
      - "80:80" 
    depends_on:
      - backend
    restart: unless-stopped